var express = require("express");
var router  = express.Router();

// Require the controllers, using the index
var controllers = require("/controllers");

// Setup the routes with one router

module.exports = router;